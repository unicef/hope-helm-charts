apiVersion: v1
kind: Service
metadata:
  name: {{ include "hope-beneficiary-portal.fullname" . }}
  labels:
    {{- include "hope-beneficiary-portal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hope-beneficiary-portal.selectorLabels" . | nindent 4 }}