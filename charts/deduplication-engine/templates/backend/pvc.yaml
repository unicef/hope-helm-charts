apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "deduplication-engine.fullname" . }}-model-data
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.modelPvc.size }}
  storageClassName: {{ .Values.modelPvc.storageClassName }}