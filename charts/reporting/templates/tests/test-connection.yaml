apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "reporting.fullname" . }}-test-connection"
  labels:
    {{- include "reporting.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "reporting.fullname" . }}:{{ .Values.backend.service.port }}']
  restartPolicy: Never
